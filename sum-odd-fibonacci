function sumFibs(num) {
  let prev = 0;
  let curr = 1;
  let output = 0;

  while (curr <= num) {
    if (curr % 2 > 0) {
      output += curr
      console.log(output)
    }
    curr += prev;
    prev = curr - prev;
  }
  // console.log(output);
  return output;
}

sumFibs(4);
